技术方案 step03 目录生成阶段，支持拖动目录排序。
- 在“添加一级目录”边上增加一个“目录排序”，点击后进入排序状态，然后三个按钮都消失，变成“保存排序”
- 只能同级排序，即一级目录排序，同父目录下的自己目录排序，不能跨父目录，也不能跨级别。
- 注意排序后要对目录重新编号，为了降低读写频率，拖动目录仅进行前端重排，只有“保存排序”，才改数据库。
- 如果已经生成了正文等后续内容，判断下是否需要做处理
- 如果用户在排序状态未保存就切换页面，询问用户“排序结果是否保存”