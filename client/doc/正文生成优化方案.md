1. 点击生成正文弹出一个配置框
  - 是否使用AI生图（开关）
    - 当前生图API可用状态（显示状态，如果不是可用状态，提示“请到设置页面配置生图模型”） 
    - 全文图片最大数量（输入数字）
  - 是否生成mermaid图片（开关）
    - 开启后提示：当前mermaid转图片使用的是（https://mermaid.ink/）的免费接口，可能不稳定，导出word后请仔细核对
2. 强化正文提示词，不要随机生成mermaid代码段
3. 在决策阶段，加入mermaid图表判断，如果内容较为简单，优先使用mermaid配图，不使用AI生图，注意mermaid图和AI生图是二选一的，不要插两张图。
4. 导出阶段mermaid转图片使用 https://mermaid.ink/ 的免费接口
5. AI图片生成放在整体决策之后，以便能均匀分布并控制最大生成数量
6. 开发者模式下，增加一个悬浮框，记录生成图片数量和成功、失败数量
7. 线段显示启用rehypeRaw，把html也渲染出来
8. 当前在 appendGeneratedImageMarkdown() 里追加：*图：xxx（AI 生成示意）*改成：*图：xxx*
9. 图片段落居中、图片后一行图例居中、Word 导出也可以同步把图例居中
10. 增加图片点击全屏查看
